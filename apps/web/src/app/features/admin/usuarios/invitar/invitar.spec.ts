// Requiere `UsuariosService` (Auth+Firestore reales, incluida la app
// secundaria de core/firebase-secondary.ts) o el Firebase Emulator Suite,
// todavía no configurado en este proyecto.
import { describe, it } from 'vitest';

describe('Invitar', () => {
  it.todo('cubrir con Firebase Emulator Suite (Auth + Firestore)');
});
