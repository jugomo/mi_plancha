// Requiere `Sesion`/`ClientesService`/`PedidosService` (Firestore real) o el
// Firebase Emulator Suite, todavía no configurado en este proyecto.
import { describe, it } from 'vitest';

describe('ClientePedidos', () => {
  it.todo('cubrir con Firebase Emulator Suite (Firestore)');
});
