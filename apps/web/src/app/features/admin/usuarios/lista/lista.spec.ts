// Requiere `UsuariosService`/`Sesion` (Auth+Firestore reales) o el Firebase
// Emulator Suite, todavía no configurado en este proyecto.
import { describe, it } from 'vitest';

describe('Lista', () => {
  it.todo('cubrir con Firebase Emulator Suite (Auth + Firestore) — incluir que uno no puede autodesactivarse');
});
