// Requiere Firestore real (config/mesas + colección mesas/) o el Firebase
// Emulator Suite, todavía no configurado en este proyecto.
import { describe, it } from 'vitest';

describe('Mesas', () => {
  it.todo('cubrir con Firebase Emulator Suite (Firestore) — incluir el caso de reducir con una mesa ocupada');
});
