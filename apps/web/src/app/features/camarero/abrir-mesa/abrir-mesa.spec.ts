// Requiere `Sesion`/`ClientesService` (Firestore real, incluida la transacción
// "abrir mesa") o el Firebase Emulator Suite, todavía no configurado en este
// proyecto.
import { describe, it } from 'vitest';

describe('AbrirMesa', () => {
  it.todo('cubrir con Firebase Emulator Suite (Firestore) — incluir el caso de mesa ya ocupada (carrera)');
});
