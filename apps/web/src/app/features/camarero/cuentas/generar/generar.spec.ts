// Requiere `Sesion`/`ClientesService`/`CuentasService` (Firestore real) o el
// Firebase Emulator Suite, todavía no configurado en este proyecto.
import { describe, it } from 'vitest';

describe('Generar', () => {
  it.todo('cubrir con Firebase Emulator Suite (Firestore) — incluir el snapshot de precios y la transacción completa');
});
