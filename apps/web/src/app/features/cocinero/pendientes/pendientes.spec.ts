// Requiere `Sesion`/`PedidosService` (Firestore real) o el Firebase Emulator
// Suite, todavía no configurado en este proyecto.
import { describe, it } from 'vitest';

describe('Pendientes', () => {
  it.todo('cubrir con Firebase Emulator Suite (Firestore) — incluir la carrera al tomar un pedido');
});
